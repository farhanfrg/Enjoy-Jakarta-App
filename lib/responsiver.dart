class resp {
  static dynamic responsiver(target, percentage) {
    return target * (percentage / 812);
  }

  static dynamic responsiverw(target, percentage) {
    return target * (percentage / 375);
  }
}
